# Redis Configuration (OPTIONAL)
# Redis is used for temporary metadata storage, caching, and rate limiting
# The application will work fine without Redis - it's an optional enhancement

# Redis URL (default: redis://localhost:6379)
# Uncomment and set if you want to use Redis
# REDIS_URL=redis://localhost:6379

# Redis password (if authentication is enabled)
# REDIS_PASSWORD=your_redis_password_here

# Note: If Redis is not running, the app will automatically disable caching features
# and continue to work normally. No errors will occur.
